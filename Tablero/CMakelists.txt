#proyecto para la lavadora
add_executable(lavadora
        main.c
        ../common/sdk_inlines.c
        )

# Pull in common dependencies
target_link_libraries(lavadora pico_stdlib)

# Create map/bin/hex file etc.
pico_add_extra_outputs(lavadora)
