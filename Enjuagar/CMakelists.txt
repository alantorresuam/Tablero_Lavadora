cmake_minimum_required(VERSION 3.13)

add_executable(Enjuagar
            Enjuagar.c
)

target_link_libraries(Enjuagar pico_stdlib)

# enable usb output, disable uart output
pico_enable_stdio_usb(Enjuagar 1)
pico_enable_stdio_uart(Enjuagar 0)

pico_add_extra_outputs(Enjuagar)